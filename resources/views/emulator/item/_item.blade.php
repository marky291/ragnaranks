<div class="tw-bg-white">
    <img src="{{ $item->image }}" alt="">
    <img src="{{ $item->icon }}" alt="">
    <h1>{{ $item->name }}</h1>
    <p>{{ $item->description }}</p>
    <p>{{ $item->script }}</p>
    <h2>{{ $item->type }} and {{ $item->subType }}</h2>
</div>
